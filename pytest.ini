[pytest]
testpaths = app
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers --tb=short -o cache_dir=/tmp/.pytest_cache
markers =
    integration: Integration tests
    unit: Unit tests
    slow: Slow tests
